/*
angular.module("avelon").config( function (ChartJsProvider) {
    ChartJsProvider.setOptions({
      colours: ["red", "blue", "green"],
//      responsive: false
      title: {display: true, text: "Moist for last two days" },
      scales: { xAxes: [{ type: "time", time: {displayFormats: {quarter: "HH:MM"}}}]},
    });

//    ChartJsProvider.setOptions('Line', {
//      datasetFill: false
//    });
});
*/

